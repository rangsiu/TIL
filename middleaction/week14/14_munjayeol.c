//
// Created by 황시우 on 25. 12. 1..
//문자열 섞기
//auto

#include <stdio.h>

void mix_print(int N, int K, char *str)
{
    char *p = str;
    int i;
    // 이 위로 수정 금지

    // 이 아래로 수정 금지
}

int main(int argc, char const *argv[])
{
    int N, K;
    char str[100] = {0};

    scanf("%d %d", &N, &K);
    scanf(" %s", str);

    mix_print(N, K, str);

    return 0;
}